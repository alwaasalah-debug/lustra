name: Supabase Keep Alive

on:
  schedule:
    - cron: '0 9 * * *' # Run every day at 9am UTC
  workflow_dispatch: # Allow manual triggering

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Supabase Database
        run: |
          # Request 1 row from the 'products' table to simulate activity
          curl -f -X GET 'https://vyxoborfafubpkizyxty.supabase.co/rest/v1/products?select=*&limit=1' \
          -H "apikey: sb_publishable_WdrwInmbQ6rn7Tbu0yNu3g_TnjUjPpf" \
          -H "Authorization: Bearer sb_publishable_WdrwInmbQ6rn7Tbu0yNu3g_TnjUjPpf"
